<table id="indicator">
  <% @arrivals.each do |arrival| %>
  <tr>
    <td class="number"><%= arrival["lineId"] %></td>
    <td><%= arrival["timeToStation"].to_i / 60 %></td>
  </tr>
  <% end %>
</table>
